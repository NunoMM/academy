package com.ctw.workstation.booking.service;

import com.ctw.workstation.booking.entity.Booking;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class BookingDatabase {

    /*
    private final Map<String, Booking> bookingMap;

    public BookingDatabase () { this.bookingMap = new HashMap<>(); }

    public void addBooking(Booking booking) { bookingMap.put(booking.getId(), booking); }

    public void removeBooking(String id) { bookingMap.remove(id); }

    public Booking getBooking(String id) { return bookingMap.get(id); }

    public Map getAllBookings() { return bookingMap; }

    public List<Booking> getBookingsByTeamId(String teamId) {
        return null;
    }

     */
}
