package com.ctw.workstation.team.service;

import com.ctw.workstation.team.entity.Team;

import java.util.HashMap;
import java.util.Map;

public class TeamDatabase {

    /*

    private final Map<String, Team> teamMap;

    public TeamDatabase() { this.teamMap = new HashMap<>(); }

    public void addTeam(Team team) { teamMap.put(team.getId(), team); }

    public void removeTeam(String id) { teamMap.remove(id); }

    public Team getTeam(String id) { return teamMap.get(id); }

    public Map getAllTeams() { return teamMap; }


     */

}
